<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : 'Relatório de Manutenção' %></title>
    <%= wicked_pdf_stylesheet_link_tag "pdf" %>

    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <div class="logo">
          <%= wicked_pdf_image_tag 'logo.png' %>
        </div>
        <div class="title">
          <h1><%= content_for?(:title) ? yield(:title) : 'Relatório de Manutenção' %></h1>
        </div>
      </div>

      <div class="content">
        <%= yield %>
      </div>

      <div class="footer">
        <p>Gerado em <%= l(Time.current, format: :long) %></p>
        <p>Página <%= page %> de <%= total_pages %></p>
      </div>
    </div>
  </body>
</html>

<%# Estilos para o PDF %>
<style>
  body {
    font-family: 'Helvetica', sans-serif;
    font-size: 12px;
    line-height: 1.4;
    color: #333;
  }

  .container {
    padding: 20px;
  }

  .header {
    display: flex;
    align-items: center;
    margin-bottom: 30px;
    border-bottom: 1px solid #ddd;
    padding-bottom: 20px;
  }

  .logo {
    width: 100px;
  }

  .title {
    flex: 1;
    text-align: center;
  }

  .title h1 {
    font-size: 24px;
    margin: 0;
    color: #2c3e50;
  }

  .content {
    margin-bottom: 30px;
  }

  .footer {
    border-top: 1px solid #ddd;
    padding-top: 20px;
    text-align: center;
    font-size: 10px;
    color: #666;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 20px;
  }

  th, td {
    border: 1px solid #ddd;
    padding: 8px;
    text-align: left;
  }

  th {
    background-color: #f5f5f5;
    font-weight: bold;
  }

  tr:nth-child(even) {
    background-color: #f9f9f9;
  }

  .status-badge {
    display: inline-block;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 10px;
    font-weight: bold;
  }

  .status-badge.success {
    background-color: #d4edda;
    color: #155724;
  }

  .status-badge.warning {
    background-color: #fff3cd;
    color: #856404;
  }

  .status-badge.error {
    background-color: #f8d7da;
    color: #721c24;
  }

  .status-badge.info {
    background-color: #d1ecf1;
    color: #0c5460;
  }
</style> 